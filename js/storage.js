const STORAGE_KEY='affixo_medcontrol_standard_v3';const STORAGE_CORRUPT_BACKUP_KEY=`${STORAGE_KEY}_corrupt_backup`;
function getState(){const raw=localStorage.getItem(STORAGE_KEY);if(raw){try{const p=JSON.parse(raw);if(p===null||typeof p!=='object'||Array.isArray(p))throw new Error('MedControl: invalid storage root.');p.settings=p.settings||{};p.medications=Array.isArray(p.medications)?p.medications:[];p.intakeLogs=Array.isArray(p.intakeLogs)?p.intakeLogs:[];if(!p.settings.interfaceLanguage||!TRANSLATIONS[p.settings.interfaceLanguage])p.settings.interfaceLanguage='en';delete p.settings.detectedLanguage;delete p.settings.languageMode;if(!p.settings.country)p.settings.country=inferCountryFromLocale();if(!p.settings.timezone)p.settings.timezone=Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC';return p}catch(err){try{localStorage.setItem(STORAGE_CORRUPT_BACKUP_KEY,raw)}catch(e){throw err}}}const initial=makeDefaultState();saveState(initial);return initial}
function saveState(state){state.settings.timezone=Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC';try{localStorage.setItem(STORAGE_KEY,JSON.stringify(state));const b=window.MedControlSupabaseBridge;if(b&&b.isReady&&b.isReady())b.queue(state);return true}catch(error){console.error('MedControl: failed to save application state.',error);return false}}