(function(){
  const APPLY={today:'Только сегодня',future:'Только на последующие дни расписания',today_future:'Сегодня и на последующие дни расписания'};
  const WEEK={Mon:'Пн',Tue:'Вт',Wed:'Ср',Thu:'Чт',Fri:'Пт',Sat:'Сб',Sun:'Вс'};
  const PERIODS={today:'Сегодня','7':'7 дней','30':'30 дней',all:'Весь период'};
  function esc(v){return escapeHtml(v===undefined||v===null||v===''?'—':String(v));}
  function ld(iso){try{return localDateFromISO(iso);}catch(_){return '';}}
  function scheduleName(s){return s?.scheduleType==='weekdays'?'Дни недели':s?.scheduleType==='explicit_dates'?'Даты':'Каждый день';}
  function scheduleParams(s){if(s?.scheduleType==='weekdays')return (s.weekdays||[]).map(x=>WEEK[x]||x).join(', ');if(s?.scheduleType==='explicit_dates')return (s.explicitDates||[]).map(formatDate).join(', ');return '';}
  function created(m){return (m.rowHistory||[]).find(e=>e?.action==='created');}
  function cutoff(p){if(p==='all')return null;const d=new Date();d.setHours(0,0,0,0);if(p==='7')d.setDate(d.getDate()-6);if(p==='30')d.setDate(d.getDate()-29);return d.getTime();}
  function within(at,p){const c=cutoff(p);return c===null||new Date(at).getTime()>=c;}
  function selector(p){return `<div class="inline" style="margin-bottom:12px"><label>Период</label><select id="timeHistoryPeriod" onchange="refreshInputTimeHistory()">${Object.entries(PERIODS).map(([v,l])=>`<option value="${v}" ${v===p?'selected':''}>${l}</option>`).join('')}</select></div>`;}
  function latestTimeRecord(m,t){return (m.timeStatusHistory||[]).filter(x=>x?.time===t||x?.oldTime===t||x?.newTime===t).sort((a,b)=>new Date(a.at||0)-new Date(b.at||0)).pop();}
  function activeNow(m,t){const r=latestTimeRecord(m,t);if(r&&typeof r.active==='boolean')return Boolean(r.active);if(m.timeStatuses&&typeof m.timeStatuses[t]==='boolean')return m.timeStatuses[t];return true;}
  function rows(m,t){
    const out=[],c=created(m),snap=c?.snapshot||m;
    if(c&&(snap.times||[]).includes(t))out.push({at:c.at,event:'Создано',scope:'',schedule:scheduleName(snap),params:scheduleParams(snap),start:snap.startDate?formatDate(snap.startDate):'—',end:snap.endDate?formatDate(snap.endDate):'—',status:'Активно'});
    const seen=new Set();
    (m.timeStatusHistory||[]).forEach(x=>{
      if(x?.time!==t&&x?.oldTime!==t&&x?.newTime!==t)return;
      const newTime=x.newTime||x.time||t,key=[x.at,x.oldTime||'',newTime,x.active,x.scope||'',x.deleted?'deleted':''].join('|');
      if(seen.has(key))return;seen.add(key);
      const fields={at:x.at,event:'Изменено',scope:x.scope&&x.scope!=='daily'?(APPLY[x.scope]||''):'',schedule:'',params:'',start:'',end:'',status:''};
      if(x.deleted){fields.event=`Время ${x.oldTime||x.time||t} удалено`;fields.params=`Удалено время ${x.oldTime||x.time||t}`;fields.status='Удалено';}
      else if(x.oldTime&&x.newTime&&x.oldTime!==x.newTime){fields.event='Изменено';fields.params=`Время изменено: ${x.oldTime} → ${x.newTime}`;fields.status=typeof x.active==='boolean'?(x.active?'Активно':'Пассивно'):'';}
      else if(typeof x.active==='boolean'){fields.status=x.active?'Активно':'Пассивно';}
      const date=x.date||ld(x.at);if(x.scope==='today'){fields.start=formatDate(date);fields.end=formatDate(date);}out.push(fields);
    });
    (m.rowHistory||[]).forEach(e=>{
      const x=e?.changes?.timeStatus;if(!x||(![x.time,x.oldTime,x.newTime].includes(t)))return;
      const newTime=x.newTime||x.time||t,key=[e.at,x.oldTime||'',newTime,x.active,x.scope||'',x.deleted?'deleted':''].join('|');if(seen.has(key))return;seen.add(key);
      out.push({at:e.at,event:x.deleted?`Время ${x.oldTime||x.time||t} удалено`:'Изменено',scope:x.scope&&x.scope!=='daily'?(APPLY[x.scope]||''):'',schedule:'',params:x.deleted?`Удалено время ${x.oldTime||x.time||t}`:(x.oldTime&&x.newTime&&x.oldTime!==x.newTime?`Время изменено: ${x.oldTime} → ${x.newTime}`:''),start:x.scope==='today'?formatDate(x.date||ld(e.at)):'',end:x.scope==='today'?formatDate(x.date||ld(e.at)):'',status:x.deleted?'Удалено':(typeof x.active==='boolean'?(x.active?'Активно':'Пассивно'):'')});
    });
    return out.sort((a,b)=>new Date(a.at)-new Date(b.at));
  }
  function render(m,t,p){const rr=rows(m,t).filter(r=>within(r.at,p));if(!rr.length)return '<p class="muted">В выбранном периоде изменений этого времени нет.</p>';return `<table><thead><tr><th>Дата/время</th><th>Событие</th><th>Область изменения</th><th>Расписание</th><th>Параметры расписания</th><th>Дата начала</th><th>Дата окончания</th><th>Статус</th></tr></thead><tbody>${rr.map(r=>`<tr><td>${esc(formatDateTime(r.at))}</td><td>${esc(r.event)}</td><td>${esc(r.scope)}</td><td>${esc(r.schedule)}</td><td>${esc(r.params)}</td><td>${esc(r.start)}</td><td>${esc(r.end)}</td><td>${esc(r.status)}</td></tr>`).join('')}</tbody></table>`;}
  window.refreshInputTimeHistory=function(){const c=window.__timeHistory,m=(getState().medications||[]).find(x=>x.id===c?.id),host=document.getElementById('rowHistoryContent');if(!c||!m||!host)return;const p=document.getElementById('timeHistoryPeriod')?.value||c.period||'all';c.period=p;host.innerHTML=selector(p)+render(m,c.time,p);};

  function dialog(){let d=document.getElementById('timeStatusEditDialog');if(!d){d=document.createElement('dialog');d.id='timeStatusEditDialog';document.body.appendChild(d);}return d;}
  window.editMedicationTimeStatus=function(id,time){const m=(getState().medications||[]).find(x=>x.id===id);if(!m)return;const r=latestTimeRecord(m,time),scope=r?.scope||'daily',active=activeNow(m,time),d=dialog();window.__timeEdit={id,time};d.innerHTML=`<h2>Изменить время ${esc(time)}</h2><div class="form-grid"><div><label>Время</label><input id="timeEditValue" type="time" value="${esc(time)}"></div><div><label>Статус</label><select id="timeEditStatus"><option value="active" ${active?'selected':''}>Активно</option><option value="passive" ${!active?'selected':''}>Пассивно</option></select></div><div class="full"><label>Параметры расписания</label><select id="timeEditScope"><option value="daily" ${scope==='daily'?'selected':''}>Без изменения расписания</option><option value="today" ${scope==='today'?'selected':''}>Только сегодня</option><option value="future" ${scope==='future'?'selected':''}>Только на последующие дни расписания</option><option value="today_future" ${scope==='today_future'?'selected':''}>Сегодня и на последующие дни расписания</option></select></div><div class="full right"><button type="button" onclick="saveInputTimeEditV2()">Сохранить</button><button type="button" onclick="document.getElementById('timeStatusEditDialog').close()">Закрыть</button></div></div>`;d.showModal();};
  window.saveInputTimeEditV2=function(){const c=window.__timeEdit,state=getState(),m=(state.medications||[]).find(x=>x.id===c?.id);if(!c||!m)return;const oldTime=c.time,newTime=document.getElementById('timeEditValue')?.value||oldTime,active=document.getElementById('timeEditStatus')?.value==='active',scope=document.getElementById('timeEditScope')?.value||'daily',oldActive=activeNow(m,oldTime),oldScope=latestTimeRecord(m,oldTime)?.scope||'daily';if(newTime===oldTime&&active===oldActive&&scope===oldScope){document.getElementById('timeStatusEditDialog')?.close();return;}if(newTime!==oldTime&&(m.times||[]).includes(newTime)){alert('Такое время уже существует.');return;}const at=nowISO(),date=currentLocalDate();if(newTime!==oldTime){m.times=(m.times||[]).map(t=>t===oldTime?newTime:t).sort();if(m.timeStatuses&&Object.prototype.hasOwnProperty.call(m.timeStatuses,oldTime)){m.timeStatuses[newTime]=m.timeStatuses[oldTime];delete m.timeStatuses[oldTime];}}if(!Array.isArray(m.timeStatusHistory))m.timeStatusHistory=[];m.timeStatusHistory.push({time:newTime,oldTime,newTime,active,scope,date,at});if(scope==='daily'||scope==='today_future'){m.timeStatuses=m.timeStatuses||{};m.timeStatuses[newTime]=active;}if(!Array.isArray(m.rowHistory))m.rowHistory=[];m.rowHistory.push({at,action:'edited',scheduleScope:scope==='daily'?null:scope,scheduleScopeLabel:scope==='today'?'Только сегодня':scope==='future'?'Только на последующие дни расписания':scope==='today_future'?'Сегодня и на последующие дни расписания':'',changes:{timeStatus:{time:newTime,oldTime,newTime,active,scope,date}},payload:'Изменены данные времени.'});saveState(state);document.getElementById('timeStatusEditDialog')?.close();mount('input');};
})();