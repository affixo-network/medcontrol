function ensureTemporalChangeState(med){if(!med.temporalChangePermissions||typeof med.temporalChangePermissions!=='object')med.temporalChangePermissions={schedule:true,time:true};if(!med.temporalPending||typeof med.temporalPending!=='object')med.temporalPending={schedule:false,time:false};return med}function temporalScheduleSignature(med){return JSON.stringify({scheduleType:med.scheduleType||'daily',weekdays:Array.isArray(med.weekdays)?[...med.weekdays].sort():[],explicitDates:Array.isArray(med.explicitDates)?[...med.explicitDates].sort():[],startDate:med.startDate||'',endDate:med.endDate||''})}function temporalTimeSignature(med){return JSON.stringify(Array.isArray(med.times)?[...med.times].sort():[])}function temporalScheduleChanged(a,b){return temporalScheduleSignature(a)!==temporalScheduleSignature(b)}function temporalTimeChanged(a,b){return temporalTimeSignature(a)!==temporalTimeSignature(b)}window.guardTemporalEdit=()=>true;window.validateTemporalEditAuthorization=(med,u)=>({scheduleChanged:temporalScheduleChanged(med,u),timeChanged:temporalTimeChanged(med,u)});window.completeTemporalEdit=med=>{ensureTemporalChangeState(med);med.temporalChangePermissions.schedule=true;med.temporalChangePermissions.time=true;med.temporalPending.schedule=false;med.temporalPending.time=false};window.beginTemporalCancellation=function(){};window.saveTemporalCancellation=function(){};window.cancelTemporalCancellationDraft=function(){};function temporalPendingNoticeHtml(){return ''}